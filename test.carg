#include <stdio.h>

fun test(int arg1, int arg2, out(int, int)) {
    if (arg1 == 0) {
        fail("ERR");
    }
    ret arg1 + arg2, arg1 * arg2;
}

fun main() {
    expand int a, int b = test(0, 2);
    printf("%d %d\n", a, b);
}