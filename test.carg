#include <stdio.h>

safe test(int arg1, int arg2, out(int, int)) {
    if (arg1 == 0) {
        fail("ERR");
    }
    ret arg1 + arg2, arg1 * arg2;
}

int main() {
    expand int a, int b = test(5, 2);
    printf("%d %d\n", a, b);
}